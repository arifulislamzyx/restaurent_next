import Registration from "@/Authentication/Registration/Registration";
import React from "react";

export const metadata = {
  title: "Registration - Restaurent Next",
};

const Page = () => {
  return (
    <>
      <Registration />
    </>
  );
};

export default Page;
